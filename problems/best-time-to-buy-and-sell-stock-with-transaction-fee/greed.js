/**
 * @param {number[]} prices
 * @param {number} fee
 * @return {number}
 */
var maxProfit = function(prices, fee) {
  let res = 0;
  let des = 0;
  for (let i = 1; i < prices.length; i++) {
    let delta = prices.length;
  }
};
